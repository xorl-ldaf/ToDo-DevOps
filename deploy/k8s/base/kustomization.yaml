apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - pod-disruption-budget.yaml
  - service.yaml
  - ingress.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: todo-web-app
      app.kubernetes.io/component: web-app
      app.kubernetes.io/part-of: todo-devops
