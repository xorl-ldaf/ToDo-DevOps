apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todo-devops-local

resources:
  - namespace.yaml
  - ../../base

# This overlay deploys only the todo-web-app workload.
# PostgreSQL, Kafka, Prometheus, and Grafana remain external prerequisites.
configMapGenerator:
  - name: todo-web-app-config
    envs:
      - configmap.env

# Placeholder values are committed for local rendering only; replace them before real cluster use.
secretGenerator:
  - name: todo-web-app-secret
    envs:
      - secret.env

images:
  - name: ghcr.io/xorl-ldaf/todo-devops
    newName: ghcr.io/xorl-ldaf/todo-devops
    newTag: local

patches:
  - path: deployment-patch.yaml
  - path: ingress-patch.yaml
