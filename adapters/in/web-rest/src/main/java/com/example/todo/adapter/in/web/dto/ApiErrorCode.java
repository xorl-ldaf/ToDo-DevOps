package com.example.todo.adapter.in.web.dto;

public enum ApiErrorCode {
    VALIDATION_FAILED,
    RESOURCE_NOT_FOUND,
    ALREADY_EXISTS,
    INVALID_STATE,
    APPLICATION_VALIDATION_FAILED,
    INTERNAL_ERROR
}
